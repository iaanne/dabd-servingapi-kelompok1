worker_processes auto;
pid /tmp/dabd-nginx-1.pid;
error_log /tmp/dabd-nginx-1-error.log warn;
events { worker_connections 4096; }
http {
  client_body_temp_path /tmp/dabd-nginx/body;
  proxy_temp_path /tmp/dabd-nginx/proxy;
  access_log /tmp/dabd-nginx/access-1.log;
  upstream app_servers {
    server 127.0.0.1:3001;
  }
  server {
    listen 8080;
    proxy_read_timeout 60s;
    location / { proxy_pass http://app_servers; }
  }
}
